User-agent: *
Allow: /
Disallow: /superficie/lab/

# Insumos do SUPERFÍCIE Reader: placas em webp, camadas de texto em JSON,
# manifesto e fragmentos de artigo. Os fragmentos são HTML sem <head> e sem
# canonical que repetem, palavra por palavra, o texto de /superficie/artigos/ —
# indexá-los criaria conteúdo duplicado competindo com a página real, e ainda
# carregando o carimbo interno de checagem editorial.
Disallow: /superficie/issues/

# O PDF da edição NÃO é exceção ao Disallow acima. `buildPlaceholderPdf()`
# gera um fallback de 10 KB — uma linha por página, sem acentuação correta —
# que existe só para o reader ter um `pdfFallback` quando o canvas falha.
# Ele já esteve com `Allow:` aqui e ficou público e indexável enquanto
# /superficie/edicao-00 dizia "download ainda indisponível". Quando houver
# uma edição em PDF de verdade, reabra a linha junto com o botão da página.

Sitemap: https://olhossecos.com.br/sitemap-index.xml
