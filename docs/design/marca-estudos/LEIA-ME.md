# Estudos de marca — referência, não entrega

Gerados com Flux 2 Pro em 19/08/2026. **São raster e não substituem nada.**

O CLAUDE.md proíbe marca gerada com IA: "sairia raster, divergente do SVG e
impossível de manter". A regra continua valendo. Estes arquivos existem só como
direção visual — se alguma agradar, o caminho é redesenhar em SVG a partir do
`EyeMark.astro`, mantendo `#087f95`.

Por isso ficam em `docs/`: não entram em `public/`, não vão para o release e não
encostam no `favicon.svg`.
